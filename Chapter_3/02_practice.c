#include <stdio.h>
int main()
{
    int num;
    printf("enter ur number \n");
    scanf("%d", &num);
    if ((num ==1) || (num ==2) || (num==3))
    {

        printf("The number consist");
    }
    else
    {
        printf("It doesn't exist");
    }
    return 0;
}
